# v0.9.2-preview.2 — WeChat Intelligence Hub public preview

这是 WeChat Intelligence Hub 的首发候选版本。它把 Rion 自有的只读 `rion-wechat-cli` 与微信个人情报库放在同一个可安装、可测试的独立仓库中。

## 包含内容

- `wechat-cli`：统一的 Agent 调用入口。
- `Rion WeChat Reader`：独立实现、本地只读的数据读取核心。
- `wechat-intelligence-hub`：联系人、主题、待回复、承诺、商机、复联和 24/48 小时简报。
- 虚构 Demo、公开样例、安装脚本、能力矩阵、隐私扫描和回归测试。

## 相比 preview.1

- 修复带时区的最新消息时间与本地时间比较时可能崩溃的问题。
- 消息入库改用唯一哈希索引去重，避免月度大批量导入反复扫描正文。
- 增加对应回归测试，并用新 Reader 完成一次月度真实数据实跑。

## 安装

```bash
git clone https://github.com/Rion-Wu-tech/wechat-intelligence-hub.git
cd wechat-intelligence-hub
./scripts/install.sh --with-sqlcipher
```

重启 Codex 后，先运行：

```text
$wechat-cli 运行 self-test，然后开始 setup；明确告诉我当前是完整数据库模式、通知预览模式，还是仍缺少访问材料。
```

没有数据库访问材料时，也可以先验证完整的情报处理链路：

```bash
bash projects/wechat-intelligence-hub/scripts/run_demo.sh
```

## 已验证

- Reader 对齐旧 `wechat-cli 1.6.19` 的 29 项只读工具和 266 个公开输入字段。
- 在一套用户本人授权的本地数据上，对照 97 个会话、4,479 条消息，并验证搜索、上下文、增量读取和当前 macOS 媒体路径。
- 月度实跑覆盖 104 位重点联系人、229 个活跃群和约 7.9 万条群消息。
- 119 项 Hub 测试和 43 项 Reader 测试通过；Hub 测试还在白名单临时发布候选中再次通过。
- 干净 Codex 安装、Skill 元数据、Shell/Python 语法和公开树隐私扫描通过。

## License

本版本采用 `AGPL-3.0-only`。个人、创作者和企业均可在遵守 AGPL 的前提下使用；闭源集成、专有发行、OEM 和白标需要另行取得商业授权。社群成员身份不会自动改变代码许可。

## 重要限制

这不是“一键读取所有人的微信”。完整历史和持续数据库读取要求用户自行提供其有权访问的本地数据库和访问材料。项目不会从微信进程提取密钥，不重签名、不注入、不 Hook 微信，也不包含任何旧 CLI 的源码或二进制。

没有完整数据库条件时，可以运行全虚构 Demo；macOS 通知预览仅是不完整的入站降级数据源，不能代表完整聊天记录。不同微信版本仍需要继续积累兼容样本。

完整边界见根目录 `NOTICE.md`、`SECURITY.md` 和 `projects/rion-wechat-reader/CAPABILITY_MATRIX.md`。
